# Practical-No-01-Image-Classification-using-CNN-and-Transfer-Learning
Implement an image classification pipeline using a Convolutional Neural Network (CNN). Train a CNN model from scratch and fine-tune a pretrained transfer learning model (such as ResNet50 or MobileNetV2) on the same dataset. Compare the performance of both models using appropriate evaluation metrics.
